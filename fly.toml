app = "cge-api-powstik"
primary_region = "iad"

[env]
  PORT = "3000"
  HOST = "0.0.0.0"

[http_service]
  internal_port = 3000
  processes = ["app"]
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  force_https = true
